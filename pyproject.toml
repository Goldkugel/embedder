[project]
name = "embedder"
version = "1.0.0"
dependencies = [
    "logger @ git+https://github.com/Goldkugel/logger.git@v1.0.5"
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]